import{useEffect,useState}from'react';import PageHeader from'../components/PageHeader';import{Badge,Button,Card,Modal}from'../components/UI';import{getJson,postJson}from'../api';import type{Unit}from'../types';
export default function Units(){const[data,setData]=useState<Unit[]>([]),[open,setOpen]=useState(false),[form,setForm]=useState({name:'',code:'',head:''});const load=()=>getJson<Unit[]>('/units').then(setData);useEffect(load,[]);const save=async()=>{await postJson('/units',form);setOpen(false);setForm({name:'',code:'',head:''});load()};return <><PageHeader title="Institutional Units" subtitle="Manage contributing schools, departments, offices and centres" actions={<Button onClick={()=>setOpen(true)}>+ Add unit</Button>}/><Card><table><thead><tr><th>Code</th><th>Unit</th><th>Unit head</th><th>Contributors</th><th>Status</th></tr></thead><tbody>{data.map(x=><tr key={x.id}><td><b>{x.code}</b></td><td>{x.name}</td><td>{x.head}</td><td>{x.contributors}</td><td><Badge tone="green">{x.status}</Badge></td></tr>)}</tbody></table></Card>{open&&<Modal title="Add institutional unit" onClose={()=>setOpen(false)}><div className="formGrid"><label className="full">Unit name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Code<input value={form.code} onChange={e=>setForm({...form,code:e.target.value})}/></label><label>Unit head<input value={form.head} onChange={e=>setForm({...form,head:e.target.value})}/></label></div><div className="modalActions"><Button variant="secondary" onClick={()=>setOpen(false)}>Cancel</Button><Button onClick={save} disabled={!form.name||!form.code}>Save unit</Button></div></Modal>}</>}
