import{useEffect,useState}from'react';import PageHeader from'../components/PageHeader';import{Card,Badge,Button,Modal}from'../components/UI';import{getJson}from'../api';import type{Mapping}from'../types';export default function MappingPage(){const[m,setM]=useState<Mapping[]>([]),[open,setOpen]=useState(false);useEffect(()=>{getJson<Mapping[]>('/mappings/CHK-10587').then(setM)},[]);return <><PageHeader title="Heading Affinity Mapping" subtitle="Map each evidence chunk to one or more report headings" actions={<Button onClick={()=>setOpen(true)}>Review mapping</Button>}/><Card><div className="mappingIntro"><div><small>Selected chunk</small><h3>CHK-10587</h3><p>Peer support sessions were conducted across all hostels. Trained peer mentors facilitated discussions on stress management, academic challenges and personal wellbeing.</p></div><Badge tone="green">Reviewed</Badge></div><table><thead><tr><th>Heading ID</th><th>Heading title</th><th>Affinity score</th><th>Assignment</th></tr></thead><tbody>{m.map(x=><tr><td>{x.headingId}</td><td>{x.headingTitle}</td><td><div className="score"><i style={{width:`${x.score*100}%`}}/>{x.score.toFixed(2)}</div></td><td><Badge tone={x.assignment==='Primary'?'green':x.assignment==='Secondary'?'blue':'orange'}>{x.assignment}</Badge></td></tr>)}</tbody></table></Card>{open&&<Modal title="Assign chunk to headings" onClose={()=>setOpen(false)}>{m.map((x,i)=><label className="checkRow"><input type="checkbox" defaultChecked={i<3}/><span>{x.headingId} {x.headingTitle}</span><b>{x.score.toFixed(2)}</b><select defaultValue={x.assignment}><option>Primary</option><option>Secondary</option><option>Supporting</option><option>Cross-reference</option></select></label>)}<div className="modalActions"><Button variant="secondary" onClick={()=>setOpen(false)}>Cancel</Button><Button onClick={()=>setOpen(false)}>Save mapping</Button></div></Modal>}</>}