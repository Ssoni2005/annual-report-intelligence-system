import{useEffect,useState}from'react';import PageHeader from'../components/PageHeader';import{Badge,Button,Card,Modal}from'../components/UI';import{getJson,postJson}from'../api';import type{Cycle}from'../types';
export default function Cycles(){const[data,setData]=useState<Cycle[]>([]),[open,setOpen]=useState(false),[form,setForm]=useState({name:'Annual Report 2026–27',year:'2026–27',status:'Planned',startDate:'2026-06-01',dueDate:'2027-08-31',structureId:'st-main'});const load=()=>getJson<Cycle[]>('/cycles').then(setData);useEffect(load,[]);const save=async()=>{await postJson('/cycles',form);setOpen(false);load()};return <><PageHeader title="Reporting Cycles" subtitle="Create, activate and archive institutional reporting periods" actions={<Button onClick={()=>setOpen(true)}>+ New cycle</Button>}/><div className="metricGrid four"><Card><strong>{data.length}</strong><span>Total cycles</span></Card><Card><strong>{data.filter(x=>x.status==='Active').length}</strong><span>Active</span></Card><Card><strong>{data.filter(x=>x.status==='Planned').length}</strong><span>Planned</span></Card><Card><strong>{data.filter(x=>x.status==='Archived').length}</strong><span>Archived</span></Card></div><Card><table><thead><tr><th>Cycle</th><th>Year</th><th>Start</th><th>Due date</th><th>Status</th></tr></thead><tbody>{data.map(x=><tr key={x.id}><td><b>{x.name}</b></td><td>{x.year}</td><td>{x.startDate}</td><td>{x.dueDate}</td><td><Badge tone={x.status==='Active'?'green':x.status==='Archived'?'blue':'orange'}>{x.status}</Badge></td></tr>)}</tbody></table></Card>{open&&<Modal title="Create reporting cycle" onClose={()=>setOpen(false)}><div className="formGrid"><label className="full">Cycle name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Reporting year<input value={form.year} onChange={e=>setForm({...form,year:e.target.value})}/></label><label>Status<select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option>Planned</option><option>Active</option></select></label><label>Start date<input type="date" value={form.startDate} onChange={e=>setForm({...form,startDate:e.target.value})}/></label><label>Due date<input type="date" value={form.dueDate} onChange={e=>setForm({...form,dueDate:e.target.value})}/></label></div><div className="modalActions"><Button variant="secondary" onClick={()=>setOpen(false)}>Cancel</Button><Button onClick={save}>Create cycle</Button></div></Modal>}</>}
